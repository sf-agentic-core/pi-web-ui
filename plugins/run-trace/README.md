# 运行轨迹（run-trace）

类 harness 的 agent 运行轨迹视图：直接显示**当前打开对话**的时间线并做分析——顶部横向泳道时间轴看全貌，点击分段看**分析**（来源/状态/耗时/占比），而不只是把对话内容再看一遍。

## 效果

```
🧭 运行轨迹 ● 实时    [写小说 ●当前] [修 bug]        搜索 [____]  ▶ 回放  EN  🗑
─────────────────────────────────────────────────────────────
09:01                                    用时 12分        09:13
输入  ▓▓────▓▓────────────────────────────────────────────
模型  ──▓▓▓▓▓▓▓▓▓▓▓▓▓────────────────────────────────────
工具  ──────▓▓▓▓▓▓─────────────────────────────────────────
──────────────────────────────┬────────────────────────────
分段列表                       │ 详情（概述/预览/原始内容/来源）
🧭 用户 · 第 1 轮 · 写个小说    │ 来源  工具 · edit
💭 思考                        │ 状态  已完成
✅ edit · novel.md · 1.2s      │ 时长  1.2s
📝 改动 1 个文件                │ 轮次  第 1 轮
🏁 …                           │ 📈 edit：累计 3 次 · 平均 1.1s ·
                               │   失败率 0% · 占工具耗时 62%
```

- **顶部泳道**：专业时间轴引擎 vis-timeline（自带 vendor，离线优先）——自适应毫秒刻度、滚轮缩放、拖拽平移、一键适应；工具段按工具名着色（读=青、写=琥珀、bash=紫，其余哈希分配，图例与色块同色），失败标红、执行中闪烁，点击色块即选中分段；vendor 缺失时回退 CDN，再失败回退手写时间轴。
- **实时流动（跟随）**：对话正在跑时，时间轴锚定「现在」——**色块逐帧平滑左移，时间刻度线钉在屏幕固定位置一动不动**（刻度数值随时间滚动），绘图区右侧 40px 处一条固定的「现在」竖线，新事件贴着它出现再往左流走；执行中的色块右端始终钉在「现在」线上、随真实时间生长。滚轮缩放不会退出跟随（按新缩放级别重新锚定），拖拽平移会暂停跟随（点「跟随」恢复），运行结束时平滑退出、内容不跳。
- **左列**：分段列表（用户/思考/回答/工具/文件/系统 chips + 时间 + 耗时）。
- **右列**：选中分段的四 tab 详情——
  - **概述**：来源/状态/时长/轮次/长度/位置 + 分析（工具段：该工具累计调用/平均耗时/失败率/耗时占比）。
  - **预览/原始内容/来源**：内容与出处（消息 key、调用 id、轮次、对话）。
- **无选中时**：对话级分析（总时长/轮数/工具调用与失败/工具耗时/最慢工具 Top/文件改动/阶段分布）。
- **回放**：按**真实时间**播放——每个块按其真实耗时停留（秒级工具一闪而过、长思考就停留这么久），播放头沿时间轴推进并自动跟随窗口；可变速（0.5/1/2/4×），可勾选「跳过空闲」压缩轮次间的长间隙；进度条拖动跳转 + 播放/暂停。
- **中英双语**，筛选（泳道开关 + 搜索）。

## 原理

- 历史全量：`host.getActiveConversation()`（当前打开对话的全部消息 + 统计）→ 打开对话即有完整时间线，历史对话切回来也有。
- 实时增量：`host.onRunEvent` 的 tool_start/tool_end 直接追加/原地更新工具段（含精确耗时/参数/结果）；message/run_* 事件触发历史重拉（去抖 300ms）+ 流式 5s / 空闲 30s 轮询。
- 耗时：assistant 时间戳是消息创建时刻（生成起点，transcript 里总紧贴上一条 toolResult）：思考/回答自该时刻向前排布，按字符量估计时长（约 50 字/秒），上限至下一条消息时刻（生成必在其前完成），超窗等比压缩，结束处衔接工具调用起点；工具段耗时 = 结果时刻 − 调用起点（纯执行，不再含生成）；瞬时/极短事件只在显示层保底 ≈2px（按当前缩放窗口换算的窄条，保证可见可点），块宽度与时间轴刻度严格对齐，放大后自动收敛回真实时长——不再用固定假宽把串行工具调用叠成一堆行。
- 初始总览自动聚焦“活跃段”：把轮次间的长空闲裁掉，真正干活的时间填满视口（多轮相当分散时回退全量，不硬裁）；「⤢ 适应」按钮仍是看全部。
- 跟随的平滑靠「CSS transform 逐帧位移 + 窗口提交」两段实现：累计位移 ≥90px 才 `setWindow` 一次（提交后同步触发 vis 整表重绘，让条目重排与 transform 归零落在同一帧，肉眼无跳变）；跟随期间隐藏 vis 自带轴/网格，刻度尺由插件自绘（位置固定、数值每秒滚动，步长从 1s 到 1 天按缩放自动选）。
- 文件段：写类工具（edit/write/patch…）成功后从**调用参数**抠路径；历史消息的 toolCall 参数同样覆盖。
- 消息原文默认不下发（单条可达 200K）：列表只带摘要，详情按段按需拉。
- 时间轴 vendor：`npm run build:runtrace-vendor`（升级 vis-timeline 后重打；产物提交进 git）。

## 限制

- 目前只覆盖 **pi 引擎**（dsh 引擎无快照/事件，视图为空态）。
- 跨重启只保留最近打开的对话（ transcript 本身是真相源，切回即重建；插件不另存）。
